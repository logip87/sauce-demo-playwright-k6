{
  "name": "lt",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "node -e \"require('fs').mkdirSync('artifacts/k6', { recursive: true })\" && k6 run ./tests/reqres-users.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/k6": "^0.57.0"
  }
}
